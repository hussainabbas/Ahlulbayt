/** Populated by `cd api && npm run quran:build` — do not commit when containing verse imports. */
import type { SurahBundle } from '../../types';

export const GENERATED_CORPUS_READY = false;

export const GENERATED_SURAHS: Record<number, SurahBundle> = {};

export const GENERATED_SURAH_COUNT = 0;
