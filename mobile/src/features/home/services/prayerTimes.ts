/** @deprecated Import from `@/core/prayer-engine` instead. */
export {
  calculatePrayerTimes,
  getNextPrayer,
  formatCountdown,
  formatPrayerTime,
  prayerTimesToSchedule,
} from '@/core/prayer-engine';

export type { PrayerName, PrayerTimeEntry, NextPrayerInfo } from '@/core/prayer-engine';
