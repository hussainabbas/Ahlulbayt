/**
 * Track Player is disabled in config — re-export stub so Metro never loads
 * react-native-track-player (crashes under RN New Architecture).
 */
export { useSahifaAudio } from './useSahifaAudio.stub';
